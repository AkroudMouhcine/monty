#include "monty.h"

/**
 * nop -  doesn’t do anything.
 * @stack: stack input
 * @line_number: int input
 * Return: void.
 */

void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
